package org.dodgybits.shuffle.android.core.model.persistence;

public interface TaskQuery {

	/**
	 * projects- explicit list, none or ignore
	 * contexts - explicit list, none or ignore
	 * top or all per context
	 * top or all per project
	 * start date range
	 * due date range
	 * created date range (for Inbox)
	 * is complete - true/false or ignore
	 * is archived - true/false or ignore
	 */
}
